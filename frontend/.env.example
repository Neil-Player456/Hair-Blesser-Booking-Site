# Public backend URL (no trailing slash). Required when the frontend is served from a different host than the API.
# Example production value:
# VITE_API_BASE_URL=https://your-api.herokuapp.com
#
# Local development (optional — defaults to http://localhost:5000 if unset):
# VITE_API_BASE_URL=http://localhost:5000
